import React, { useEffect, useState } from "react";

export const App: React.FC = () => {
    return (
        <div style={{ fontFamily: "sans-serif", padding: "1.5rem" }}>
            <h1>EkLine Document App (Skeletons)</h1>
        </div>
    );
};
